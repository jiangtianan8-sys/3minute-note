管道：将一个命令的输出传送给另一个命令，作为另一个命令的输入。

使用方法：

        命令 1｜命令 2｜……….｜命令 N

使用举例：

        ls -RI /etc | more

        cat /etc/passwd | wc

        cat /etc/passwd | grep lrj

        dmesg | grep eth0

        man bash | col -b > bash.txt

        ls -l | grep "^d"

        ls -l * | grep "^-" | wc -l
