1.历史命令：history

历史命令的调用

       使用上，下箭头调用以前的历史命令

       使用 !n 重复执行第 n 调历史命令

       使用 !! 重复执行上一条命令

       使用 !字串  重复执行最后一条以该字串开头的命令

2.命令补全

在 Bash 中，命令与文件补全是非常方便与常用的功能，我们只要在输入命令或文件时，

按 Tab 键就会自动进行补全
