用**JavaScript + p5.js(Canvas）**实现“自动寻路版贪吃蛇"。
请输出**单个可直接运行的 index. html **（完整 <html>w< /html> 内联样式与脚
本），仅从 CDN 加载 p5JS (无需其他资源/库）。
功能要求（简洁高效）
1）**网格**：20x20(或可调），蛇与食物按格移动；每帧固定步长（如10 FPS)。
2）**自动寻路**：实现**Al***（或 BFS)在网格上从蛇头到食物的最短路径；**每步重算
**。若无路可达，采用“安全退化策略”：选择不撞墙/不撞自身的合法方向（优先保持离尾
部更远）
3）**规则**：吃到食物后增长1节并加分；撞到墙/身体判负；食物随机生成（不可生成在
蛇或障碍上）
4）**障碍**：随机生成少量墙块（保证初始可解）；可用键R 重新开局P 暂停。
5)**显示**：简洁方格渲染；显示分数/状态；窗口缩放保持方格等比居中。
6)**结构**：拆分为 setup () /draw() GridSnakeAStar（或 BFS)
spawnFood()restart(） 等函数/类，代码清晰可读并含少量注释。
7)**性能**：路径搜索在20x20 上流畅（避免不必要的重复分配/深拷贝）

请使用 HTML5 + JavaScript + Threeijs实现一个简化版的 Minecraft 游戏，要求代码完
整、可运行、结构清晰。